<section>
    <div>
    <img src="/images/{{filename}}" alt="{{product_name}}">
    <h2>{{product_name}}</h2>
    <p>price:{{price}}</p>
    <p>stock:{{stock}}</p>
    <p>category id:{{category_id}}</p>
    </div>
</section>